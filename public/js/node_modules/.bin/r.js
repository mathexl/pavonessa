#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../requirejs/bin/r.js?v17" "$@"
  ret=$?
else 
  node  "$basedir/../requirejs/bin/r.js?v17" "$@"
  ret=$?
fi
exit $ret
